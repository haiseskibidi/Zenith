# Глава 10. Физический Viewmodel, 3D-пикинг костей (Ray-OBB) & Магнитный Лут

Приветствую тебя, дорогой коллега! Ну что, пристегни ремни: сегодня мы погрузимся в мир чистой пространственной математики, векторов и физического взаимодействия!

Мы разберем, как устроен трехмерный клик мыши по деталям скелета в нашем редакторе анимаций (**Animation Studio**), почему для пересечения луча с наклонными коробками (OBB) не нужно писать дикие тригонометрические формулы, как работает умный физический магнит для предметов с упреждением траектории (**Lead Pursuit Physics**), и как локализовать жар и раскаление на костяшках рук игрока без перетекания свечения на инструменты!

Все это — мега-понятно, на пальцах и в нашем фирменном стиле Mathprofi! Поехали!

---

## 1. 3D-пикинг Oriented Bounding Box (Ray-OBB): Укрощаем Пространство

В нашем встроенном редакторе анимаций (**Animation Studio**, запускается по клавише F8) ты можешь кликать мышкой прямо по трехмерным костям рук персонажа или деталям удерживаемого топора, чтобы выделять их и вращать.

Но как компьютер понимает, по какой именно кости кликнул игрок на плоском 2D-экране монитора? 
Для этого используется **Raycasting (Трассировка лучей)**. Мы выпускаем виртуальный луч из курсора мыши вглубь трехмерного мира.

### Ужас Oriented Bounding Box (OBB)
Если бы наши кости рук висели ровно по осям X, Y, Z, задача была бы тривиальной — это пересечение луча с выровненной коробкой (AABB). Но руки согнуты, пальцы наклонены, топор повернут по кватерниону во все стороны!
Каждый куб модели становится **Oriented Bounding Box (Ориентированной коробкой, OBB)**.

Математика прямого пересечения луча с наклонной коробкой в 3D пространстве — это настоящий ад с кучей скалярных произведений осей и систем уравнений.

```
       Глобальное Пространство (Сложно)            Локальное Пространство (Изи!)
          (Луч под углом летит к OBB)                 (Коробка выровнена, луч повернут)
          
                 \  /---/                                       \
                  \/   /                                         \   +-------+
                  /\--/  <-- Наклонено!                           \  | AABB  |
                 /                                                 ▼ +-------+
```

### Наше спасение: Относительность Эйнштейна
Вместо того чтобы решать сложные уравнения для наклонной коробки в глобальном мире, мы пользуемся принципом относительности: **мы переносим сам луч в локальное пространство кости!**

Мы берем глобальную матрицу трансформации кости `p.globalMatrix`, которая содержит все её повороты, смещения и масштабы, и **инвертируем её**.
Инвертированная матрица делает обратное действие: она возвращает любую точку пространства назад в начало координат $(0, 0, 0)$, выравнивая кость по осям!

Если мы умножим координаты нашего луча на эту инвертированную матрицу, кость станет ровной (превратится в простой AABB), а наш луч развернется в локальном пространстве под нужным углом. И мы сможем применить простейший, сверхбыстрый алгоритм пересечения луча с осями координат!

Давай посмотрим, как элегантно это реализовано в нашем контроллере:

```java
// com.za.zenith.engine.graphics.ui.editor.animation.TransformController.java
public boolean pickPart3D(float mx, float my, int sw, int sh, AnimationEditorState state, List<ModelNode> allParts) {
    Vector3f ro = new Vector3f(), rd = new Vector3f();
    // 1. Генерируем 3D-луч из экранных координат мыши
    getRay(mx, my, sw, sh, state, ro, rd);
    
    ModelNode best = null; 
    float minD = Float.MAX_VALUE;

    for (ModelNode p : allParts) {
        if (p.def == null || p.def.cubes == null) continue;

        // 2. Инвертируем глобальную матрицу трансформации этой кости!
        Matrix4f invG = new Matrix4f(p.globalMatrix).invert();

        // 3. Умножаем луч на инвертированную матрицу! 
        // ro (Origin) умножается как Точка (Position), rd (Direction) - как Направление!
        Vector3f lro = ro.mulPosition(invG, new Vector3f());
        Vector3f lrd = rd.mulDirection(invG, new Vector3f());
        
        for (BoneDefinition.CubeDefinition c : p.def.cubes) {
            // Переводим размеры куба в метры (16 пикселей = 1 метр)
            float minX = c.x / 16.0f;
            float minY = c.y / 16.0f;
            float minZ = -(c.z / 16.0f + c.size[2] / 16.0f);
            
            Vector3f min = new Vector3f(minX, minY, minZ);
            Vector3f max = new Vector3f(min).add(new Vector3f(c.size[0]/16f, c.size[1]/16f, c.size[2]/16f));
            
            Vector2f res = new Vector2f();
            // 4. Пересекаем локальный луч с локальным AABB! 0 тяжелых тригонометрических расчетов!
            if (Intersectionf.intersectRayAab(lro, lrd, min, max, res)) {
                float t = res.x < 0 ? res.y : res.x;
                if (t > 0 && t < minD) { 
                    minD = t; 
                    best = p; 
                }
            }
        }
    }
    
    if (best != null) { 
        state.selectedPart = best; // Успешно выделили кость кликом мыши!
        return true; 
    }
    return false;
}
```
**Это просто шедевр линейной алгебры!** Мы обманули геометрию, инвертировали пространство и сделали расчет клика невероятно дешевым для процессора (всего пара быстрых умножений векторов на матрицу!).

---

## 2. Магнитное Притягивание с упреждением (Lead Pursuit Physics)

Когда из сломанного сундука или рудного блока выпадает предмет (например, кусок меди или изолента), он красиво падает на землю, а затем плавно и сочно притягивается магнитом к игроку, если у игрока в инвентаре есть **Магнитный Компонент** (`MagneticComponent`).

### Проблема глупого притяжения:
Большинство разработчиков пишут притяжение в лоб:
`Vector3f dir = player.pos - item.pos; item.velocity += dir * speed;`
Но если игрок быстро бежит или прыгает, предмет летит на старое место, промахивается, начинает дико кружить вокруг игрока по орбите и никак не может попасть в карман. Это выглядит невероятно багованно!

### Наше решение: Упреждающее преследование (Lead Pursuit)
В физическом движке **Zenith** магнитное притяжение предметов учитывает скорость самого игрока!
1. Мы берем текущую скорость движения игрока (`player.getVelocity()`) и делаем её **базовой скоростью** для притягивающегося предмета.
2. Прибавляем к ней вектор направления на игрока, умноженный на квадратичную скорость сближения.
3. Это гарантирует, что предмет летит не в точку, где игрок *был*, а наследует его инерцию, идеально влетая прямо в карман на любой скорости бега!

```java
// com.za.zenith.entities.ItemEntity.java (Упрощенный фрагмент)
if (isLockedOnPlayer) {
    com.za.zenith.world.items.component.MagneticComponent magnet = 
        player.getInventory().getActiveComponent(com.za.zenith.world.items.component.MagneticComponent.class);
    
    if (magnet != null) {
        float distance = (float)Math.sqrt(distSq);
        Vector3f dirToPlayer = new Vector3f(dx, dy, dz).normalize();
        
        // Квадратичное увеличение скорости сближения по мере приближения к игроку!
        float approachSpeed = 12.0f + (1.0f - Math.min(1.0f, distance / 4.0f)) * (magnet.attractionForce * 0.2f);
        
        // Наследуем скорость игрока (Lead Pursuit) + притягиваемся!
        velocity.set(player.getVelocity());
        velocity.add(dirToPlayer.mul(approachSpeed));
        onGround = false; // Отрываем предмет от земли!
        
        // Бешеное визуальное закручивание в воздухе при полете к игроку!
        angularVelocity.y += deltaTime * 20.0f;
        angularVelocity.x += deltaTime * 5.0f;
    }
}
```
**Результат:** Предметы отрываются от земли, начинают бешено и сочно вращаться вокруг своей оси и плавно, по красивейшей упреждающей траектории влетают точно в грудь игроку, издавая тактильный щелчок подбора. Это создает потрясающее физическое ощущение контроля над миром!

[WIDGET:MAGNETIC_PICKUP]

---

## 3. Mining Heat VFX: Раскаленные костяшки рук

При добыче ресурсов в мире, если игрок долго машет киркой или бьет блоки кулаком, его инструмент и руки начинают плавно раскаляться от тяжелой работы, наливаясь сочным оранжево-красным шейдерным свечением.

### Ловушка «Глобального тепла» (Glow Transfer Bug)
Если хранить одно общее значение тепла для рук и инструмента, возникнет некрасивый баг. Допустим, ты долго бил камень рукой, твои кулаки раскалились докрасна. Ты берешь в руку ледяной железный топор — и он мгновенно загорается красным жаром, как будто только что лежал в горне!

### Решение: Разделение тепловых каналов
В менеджере визуальных эффектов `MiningVFXManager.java` мы полностью изолировали тепло рук и предметов:

```java
// com.za.zenith.engine.graphics.vfx.MiningVFXManager.java
public class MiningVFXManager {
    private float handHeat = 0.0f;  // Жар голых рук
    private float itemHeat = 0.0f;  // Жар инструмента
    
    private int lastItemIdentity = -1;

    public void update(float deltaTime, Player player, MiningController mining) {
        ItemStack currentStack = player.getInventory().getSelectedItemStack();
        int currentIdentity = (currentStack != null) ? System.identityHashCode(currentStack) : 0;
        boolean hasItem = (currentStack != null && currentStack.getItem() != Items.HAND);

        // --- Умный сброс (Reset) при смене предмета ---
        if (currentIdentity != lastItemIdentity) {
            itemHeat = 0.0f; // Новый взятый предмет всегда холодный!
            if (mining.getBreakingBlockPos() != null) mining.reset();
        }
        lastItemIdentity = currentIdentity;

        float progress = (mining.getBreakingBlockPos() != null) ? mining.getBreakingProgress() : 0.0f;
        
        // Разделяем цели: нагреваем только то, чем сейчас бьем!
        float targetHand = (!hasItem) ? progress : 0.0f;
        float targetItem = (hasItem) ? progress : 0.0f;

        // Плавный разогрев (скорость 2.0) и медленное остывание (скорость 0.5)
        handHeat = updateChannel(handHeat, targetHand, deltaTime);
        itemHeat = updateChannel(itemHeat, targetItem, deltaTime);
    }

    private float updateChannel(float current, float target, float dt) {
        if (current < target) return Math.min(target, current + dt * 2.0f);
        return Math.max(target, current - dt * 0.5f);
    }
}
```

### Локализованное свечение во фрагментном шейдере
Мы не просто красим всю модель в красный цвет. В шейдере рук мы используем маску весов костей: жар проявляется только на костяшках пальцев, где идет физический удар. В шейдере предметов мы используем вертикальную маску Y — раскаляется только рабочая металлическая часть кирки/топора, а деревянная рукоятка остается холодной!

Это внимание к микродеталям превращает инди-игру в настоящий AAA-шедевр визуального дизайна!

Увидимся в следующих главах, коллега! Твой болид готов к заезду на 100%!
