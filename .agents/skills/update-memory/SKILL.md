---
name: update-memory
description: Автоматическая синхронизация памяти проекта. Используй этот навык после любого изменения кода, архитектуры или лора, чтобы гарантировать актуальность файлов в .gemini/memory/ ТОЛЬКО С РАЗРЕШЕНИЯ ПОЛЬЗОВАТЕЛЯ.
---

# Update Memory Workflow

Этот навык обеспечивает поддержание Single Source of Truth (SSOT) проекта Zenith.

## Когда использовать
- **ТОЛЬКО ПОСЛЕ** того, как пользователь подтвердил, что новый функционал или исправление работает корректно.
- Сначала попроси пользователя протестировать изменения, и только после его одобрения активируй этот навык.
- После завершения реализации новой фичи.
- После исправления бага.
- После рефакторинга кода (обновление `code_map.md`).
- После изменения игровых механик или лора.

## Процедура обновления

### 1. Текущее состояние (current_state.md)
- Перемести выполненные задачи из "В работе" в "Реализованные фичи" (с кратким описанием).
- Добавь новые задачи в раздел "В работе".
- Отрази прогресс по Milestone в разделе Roadmap.

### 2. Архитектура (architecture.md)
- Отрази новые зависимости между пакетами.
- Добавь описание новых систем (например, "Система Энергии").
- Обнови схемы взаимодействия, если они изменились.

### 3. Карта кода (code_map.md)
- Если были добавлены новые классы или изменены сигнатуры важных методов, обнови соответствующий раздел.
- Сохраняй формат: Путь к файлу, Назначение, Функции, Зависимости.

### 4. Лор и механики (lore.md)
- Уточняй параметры механик (например, значения уровней шума).
- Добавляй новые детали сюжета.

### 5. Граф знаний MCP (memory)
- После любого значимого изменения в архитектуре (новые системы, изменение иерархии классов) обнови граф знаний.
- Используй `create_entities` для новых классов/интерфейсов.
- Используй `create_relations` для новых связей (manages, renders, inherits, implements, connects).
- Используй `add_observations` для дополнения данных об объектах.
- Убедись, что граф знаний соответствует изменениям в `architecture.md` и `code_map.md`.

### 6. Версионирование (Git)
- После успешного обновления всех файлов памяти и кода, **ОБЯЗАТЕЛЬНО** напиши (сделай коммит) изменений в Git.
- Сообщение коммита должно быть на **английском языке**, кратким, лаконичным и в **настоящем времени** (imperative mood). Пример: `Add stump crafting mechanic` или `Refactor item system to use components`.
- Перед коммитом всегда проверяй статус измененных файлов через `git status` и `git diff`.

## Принципы
- **Компактность:** Не пиши "воду", только технические факты.
- **Хирургические правки:** При обновлении файлов используй точечные изменения (replace). Избегай полной перезаписи (write_file) существующих md-файлов, чтобы сохранить историю и структуру.
- **Своевременность:** Обновляй файлы СРАЗУ после выполнения основного действия.
- **Синхронизация:** Гарантируй, что данные в `.md` файлах и графе знаний MCP не противоречат друг другу.
- **Подтверждение:** Никогда не обновляй память вслепую. Сначала дождись "ОК" или "Работает" от пользователя после тестирования.
- **Точность:** Любое утверждение в памяти должно соответствовать реальному коду.
